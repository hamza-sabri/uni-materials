export type location = {
  neededLocation: string;
  deleteLocation: any;
};

export type locations = {
  doc: any;
};
export type uniResults = {
  uniManagerResult?: any;
  setUniManagerResult?: React.Dispatch<React.SetStateAction<any>>;
};
